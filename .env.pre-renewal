# Runtime profile
GAME_MODE=pre-renewal
PACKETVER=20141022
RENEWAL=false

# Database
GAME_DB_NAME=ragnarok
MYSQL_HOST=db
DB_PORT=3306
MYSQL_USER=ragnarok
MYSQL_PASSWORD=ragnarok
MYSQL_ROOT_PASSWORD=R00t+

# rAthena network
RATHENA_HOST=serve
RATHENA_BIND_IP=0.0.0.0
LOGIN_PORT=6900
CHAR_PORT=6121
MAP_PORT=5121

# Public services
WEB_PORT=8080
FLUXCP_PORT=8081
WSPROXY_PORT=5999
PHPMYADMIN_PORT=9000

# FluxCP
FLUX_INSTALLER_PASSWORD=ragnarok
FLUX_SERVER_ADDRESS=localhost:8081
FLUX_BASE_URI=
FLUX_FORCE_HTTPS=0
FLUX_REQUIRE_OWNERSHIP=0

#docker compose --env-file .env.pre-renewal up -d --build